<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itlbw.mapper.EmpMapper">
    <select id="getTotal" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM emp;
    </select>

    <select id="getEmps" resultType="com.itlbw.pojo.Emp">
        select emp.*,dept.name as deptName from emp,dept
        where emp.dept_id = dept.id order by emp.update_time desc limit #{start},#{pageSize};
    </select>

    <select id="list" resultType="com.itlbw.pojo.Emp">
        SELECT emp.*,dept.name AS deptName FROM emp LEFT JOIN dept
            ON emp.dept_id = dept.id
            <where>
                <if test="name != null and name != ''">
                    emp.`name` LIKE concat('%',#{name},'%')
                </if>
                <if test="gender != null">
                    AND emp.gender = #{gender}
                </if>
                <if test="begin != null and end != null">
                    AND emp.entry_date BETWEEN #{begin} AND #{end}
                </if>

            </where>

            ORDER BY emp.update_time DESC
    </select>


</mapper>