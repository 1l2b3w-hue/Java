package com.homework;

public class homework12 {
    //equals : Object类上的一个方法，对于基本数据类型，无法进行判断
    // 默认是判断两个对象是不是同一个对象，在其子类中进行重写，判断实例对象的属性是不是全部相同，
    // == : 比较运算符 对于引用类型，判断两个对象是不是同一个对象 对于基本数据类型，比较值是否相等
}
